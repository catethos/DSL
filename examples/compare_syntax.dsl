# Compare let syntax vs as syntax
# Both should produce the same result

# Using let syntax
let x = 42
let message = "Hello from let syntax"
let numbers = [1, 2, 3, 4, 5]

# The above is equivalent to:
# 42 as x
# "Hello from let syntax" as message
# [1, 2, 3, 4, 5] as numbers
